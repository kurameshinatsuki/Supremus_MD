const { zokou } = require('../framework/zokou');


zokou(
    {
        nomCom: 'colisee',
        categorie: 'Arena'
    },
    async (dest, zk, commandeOptions) => {
        const { repondre, arg, ms } = commandeOptions;

        if (!arg || arg.length === 0)  {
            const lien = '';
            const msg = ``;
            zk.sendMessage(dest, { image: { url: 'https://telegra.ph/file/bca463ce8aca97c17c1e8.jpg' }, caption: msg }, { quoted: ms });
   
        }
    }
);

zokou(
    {
        nomCom: 'forteresse',
        categorie: 'Arena'
    },
    async (dest, zk, commandeOptions) => {
        const { repondre, arg, ms } = commandeOptions;

        if (!arg || arg.length === 0)  {
            const lien = '';
            const msg = ``;
            zk.sendMessage(dest, { image: { url: 'https://telegra.ph/file/751fab407e238a8db459b.jpg' }, caption: msg }, { quoted: ms });
   
        }
    }
);

zokou(
    {
        nomCom: 'temple',
        categorie: 'NEOverse'
    },
    async (dest, zk, commandeOptions) => {
        const { repondre, arg, ms } = commandeOptions;

        if (!arg || arg.length === 0)  {
            const lien = '';
            const msg = ``;
            zk.sendMessage(dest, { image: { url: 'https://telegra.ph/file/e107add6a567c0e4d6d97.jpg' }, caption: msg }, { quoted: ms });
   
        }
    }
);

zokou(
    {
        nomCom: 'lacducristal',
        categorie: 'Arena'
    },
    async (dest, zk, commandeOptions) => {
        const { repondre, arg, ms } = commandeOptions;

        if (!arg || arg.length === 0)  {
            const lien = '';
            const msg = ``;
            zk.sendMessage(dest, { image: { url: 'https://telegra.ph/file/ffb37cee1a270233193f0.jpg' }, caption: msg }, { quoted: ms });
   
        }
    }
);

zokou(
    {
        nomCom: 'parcurbain',
        categorie: 'Arena'
    },
    async (dest, zk, commandeOptions) => {
        const { repondre, arg, ms } = commandeOptions;

        if (!arg || arg.length === 0)  {
            const lien = '';
            const msg = ``;
            zk.sendMessage(dest, { image: { url: 'https://telegra.ph/file/d6be8bba00e1271d3938d.jpg' }, caption: msg }, { quoted: ms });
   
        }
    }
);

zokou(
    {
        nomCom: 'clairiere',
        categorie: 'Arena'
    },
    async (dest, zk, commandeOptions) => {
        const { repondre, arg, ms } = commandeOptions;

        if (!arg || arg.length === 0)  {
            const lien = '';
            const msg = ``;
            zk.sendMessage(dest, { image: { url: 'https://telegra.ph/file/f4cd1cbde6cb6447e7d0a.jpg' }, caption: msg }, { quoted: ms });
   
        }
    }
);

zokou(
    {
        nomCom: 'marche',
        categorie: 'Arena'
    },
    async (dest, zk, commandeOptions) => {
        const { repondre, arg, ms } = commandeOptions;

        if (!arg || arg.length === 0)  {
            const lien = '';
            const msg = ``;
            zk.sendMessage(dest, { image: { url: 'https://telegra.ph/file/7480b5430a9f1dc2ee464.jpg' }, caption: msg }, { quoted: ms });
   
        }
    }
);

zokou(
    {
        nomCom: 'champ',
        categorie: 'Arena'
    },
    async (dest, zk, commandeOptions) => {
        const { repondre, arg, ms } = commandeOptions;

        if (!arg || arg.length === 0)  {
            const lien = '';
            const msg = ``;
            zk.sendMessage(dest, { image: { url: 'https://telegra.ph/file/50a6a31e7a87162223f20.jpg' }, caption: msg }, { quoted: ms });
   
        }
    }
);

zokou(
    {
        nomCom: 'campement',
        categorie: 'Arena'
    },
    async (dest, zk, commandeOptions) => {
        const { repondre, arg, ms } = commandeOptions;

        if (!arg || arg.length === 0)  {
            const lien = '';
            const msg = ``;
            zk.sendMessage(dest, { image: { url: 'https://telegra.ph/file/bc0b0acff58c5b2c1f793.jpg' }, caption: msg }, { quoted: ms });
   
        }
    }
);

zokou(
    {
        nomCom: 'territoire',
        categorie: 'Arena'
    },
    async (dest, zk, commandeOptions) => {
        const { repondre, arg, ms } = commandeOptions;

        if (!arg || arg.length === 0)  {
            const lien = '';
            const msg = ``;
            zk.sendMessage(dest, { image: { url: 'https://telegra.ph/file/332712053671bfcc13c5a.jpg' }, caption: msg }, { quoted: ms });
   
        }
    }
);
