/*const { zokou } = require('../framework/zokou');

zokou(
    {
        nomCom: 'territoire',
        categorie: 'Arena'
    },
    async (dest, zk, commandeOptions) => {
        const { repondre, arg, ms } = commandeOptions;

        if (!arg || arg.length === 0)  {
            const lien = '';
            const msg = ``;
            zk.sendMessage(dest, { image: { url: 'https://telegra.ph/file/332712053671bfcc13c5a.jpg' }, caption: msg }, { quoted: ms });

        }
    }
);*/